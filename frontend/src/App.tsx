function App() {
  return (
    <div className="min-h-screen bg-gray-50">
      <header className="bg-white shadow-sm">
        <div className="max-w-7xl mx-auto px-4 py-4">
          <h1 className="text-2xl font-bold text-gray-900">Hotel Booking</h1>
        </div>
      </header>

      <main className="max-w-7xl mx-auto px-4 py-8">
        {/*
          Build your booking UI here.

          Suggested features (implement what you can in the time available):

          1. Display available rooms for selected dates
          2. Date range picker for check-in / check-out
          3. Room cards showing details and availability
          4. Booking form when a room is selected
          5. Success/error handling for bookings

          The API client is ready in src/api/client.ts
          Types are defined in src/types/index.ts
        */}
        <p className="text-gray-600">Start building here...</p>
      </main>
    </div>
  );
}

export default App;
